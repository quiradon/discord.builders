<%
const componentLines = `[\n${data.comp.components.map(comp =>
  indent(include('/dressed/section_textdisplay.ts.ejs', { comp }), 4)
).join(',\n')}\n]`;

const accessory = include('/dressed/components.ts.ejs', { comp: data.comp.accessory });
%>Section(
<%- indent(componentLines, 4) %>,
<%- indent(accessory, 4) %>
)